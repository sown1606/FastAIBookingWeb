# Demo Readiness Report - 2026-07-04

Demo status: READY for API wait-prompt coverage.

## API Wait Prompt Coverage

| Operation | File/flow | Wait prompt | Mechanism | Timeout/failure fallback | Tested result |
|---|---|---|---|---|---|
| Customer phone lookup / known caller lookup | `infra/aws/connect/contact-flows/ai-reception.json`; `infra/lambda/booking-handler/index.mjs`; `apps/api/src/modules/ai/ai.service.ts` | `Please wait a moment while I pull up your information.` when a slow backend lookup is needed; demo Kiet lookup is handled from Connect/Lambda attributes without backend wait. | Connect attributes / Lambda response; backend calls covered by Lex fulfillment updates. | Lambda API timeout is 2800 ms by default; backend failures return caller-safe retry/escalation text. | YES - `npm run test:lambda`; `npm --prefix apps/api test -- --test-name-pattern "AI internal|staff DTMF|new active bookable staff|successful booking|confirmed booking retry|backend errors|explicit human intent|cancel and reschedule"`. |
| Service lookup / service matching | `infra/aws/lex/FastAIBookingBot-v10/BotLocales/en_US/Intents/BookAppointmentIntent/Intent.json`; `infra/lambda/booking-handler/index.mjs`; `apps/api/src/modules/ai/ai.service.ts` | `Please wait a moment while I check the schedule.` | Lex fulfillment update for fulfillment backend calls; Lambda logs `booking_fulfillment_availability_and_creation`. | Timeout/failure elicits a safe slot retry for booking; escalation intents route to operator fallback. | YES - API tests cover service DTMF and unclear service matching without booking/escalation. |
| Staff lookup | `infra/lambda/booking-handler/index.mjs`; `apps/api/src/modules/ai/ai.service.ts` | `Please wait a moment while I find the available staff.` | Lambda backend call metadata and 2800 ms timeout guard for DialogCodeHook staff option generation; final staff resolution is covered by Lex fulfillment updates. | Slow staff option generation aborts before a 3 second silent gap and re-prompts for staff; backend failures do not create appointments. | YES - Lambda slow staff simulation uses a 3500 ms backend delay and verifies timeout before 3200 ms plus the staff wait prompt header. |
| Staff DTMF options generation | `infra/lambda/booking-handler/index.mjs`; `apps/api/src/modules/ai/ai.service.ts` | `Please wait a moment while I find the available staff.` | Lambda response / timeout guard, with `X-FastAIBooking-Wait-Operation: staff_dtmf_options_generation`. | Timeout falls back to the staff prompt; invalid DTMF repeats options without booking. | YES - Lambda and API tests cover dynamic options, invalid DTMF, DTMF 0 any staff, DTMF 3 Kelly, and newly added staff Lina. |
| Availability lookup | `infra/aws/lex/FastAIBookingBot-v7`; `infra/aws/lex/FastAIBookingBot-v8`; `infra/aws/lex/FastAIBookingBot-v10`; `apps/api/src/modules/ai/ai.service.ts` | `Please wait a moment while I check the schedule.` | Lex fulfillment update in every retained export: `active=true`, `startResponse.delayInSeconds=1`, `updateResponse.frequencyInSeconds=3`. | No availability returns spoken alternatives; API timeout/failure returns safe retry/escalation behavior. | YES - Lambda slow booking simulation delays backend 3200 ms and verifies Lex progress coverage metadata; API tests cover busy staff alternatives. |
| Appointment creation | `infra/aws/lex/FastAIBookingBot-v7`; `infra/aws/lex/FastAIBookingBot-v8`; `infra/aws/lex/FastAIBookingBot-v10`; `apps/api/src/modules/ai/ai.service.ts` | `Please wait while I create your appointment.` | Lex fulfillment update repeat message every <= 3 seconds in every retained export; Lambda logs `booking_fulfillment_availability_and_creation`. | Backend failure does not fake success; booking retries on the same Amazon Connect contact return the existing appointment instead of creating a duplicate. | YES - slow booking simulation, successful booking test, and confirmed retry idempotency test pass. |
| Appointment update/reschedule/cancel | `infra/aws/lex/FastAIBookingBot-v7`; `infra/aws/lex/FastAIBookingBot-v8`; `infra/aws/lex/FastAIBookingBot-v10`; `infra/lambda/booking-handler/index.mjs` | `Please wait while I look up your appointment.` | Lex fulfillment update with 1 second start and 3 second repeat in every retained export; Lambda operation names `appointment_cancel_lookup` and `appointment_reschedule_lookup`. | Backend failure routes to safe human escalation fallback; current voice flow asks for confirmation before handoff and does not mutate appointments directly. | YES - Lambda and API tests cover cancel/reschedule context without transfer until caller confirmation. |
| SMS/notification send | `apps/api/src/modules/appointments/appointments.service.ts` | `Please wait while I create your appointment.` | Covered inside appointment creation fulfillment progress; notifications are non-blocking after appointment creation. | Notification failures are caught and logged without blocking the caller or duplicating appointment creation. | YES - appointment creation tests pass; notification failure path remains non-blocking by service code. |
| Operator / human escalation | `infra/aws/lex/FastAIBookingBot-v7`; `infra/aws/lex/FastAIBookingBot-v8`; `infra/aws/lex/FastAIBookingBot-v10`; `infra/aws/connect/contact-flows/human-escalation.json`; `infra/lambda/booking-handler/index.mjs` | `Please wait while I connect you.` | Lex fulfillment update plus Connect human-escalation prompt before queue transfer. | If no agents are available, caller hears `No agents available.` and transfer attributes are disabled. | YES - Lambda and API tests confirm exact handoff message, no-agent fallback, and Connect prompt-before-transfer order. |
| Any backend API call from Lambda / Lex / Connect | `infra/lambda/booking-handler/index.mjs`; `apps/api/src/modules/ai/ai.routes.ts` | Operation-specific prompt in `WAIT_PROMPTS` or Lex fulfillment update. | Single Lambda helper adds wait operation/prompt headers, 2800 ms timeout guard, and structured coverage logs; API route logs operation, wait prompt, duration, success/failure, and contact/session id. | Timeout/failure never returns fake success; booking failures retry safely or escalate; confirmed booking retry is idempotent. | YES - full `npm test`, API typecheck, API build, Lambda syntax check, and JSON validation passed. |

Verification notes:

- Lex v7, v8, and v10 exports verified: fulfillment updates are active, booking start delay is 1 second, booking update frequency is 3 seconds, human escalation start delay is 1 second, and human escalation update frequency is 3 seconds.
- Slow staff lookup simulation: backend planned delay 3500 ms, Lambda timeout 2800 ms, caller path falls back before a > 3 second silent gap.
- Slow booking simulation: backend delay 3200 ms, Lex fulfillment progress provides schedule/create prompts while Lambda waits.
- Duplicate prevention: a repeated confirmed booking with the same Amazon Connect contact id returns the existing appointment and keeps appointment count at 1.
- Full verification passed: `npm test`, `npm --prefix apps/api run typecheck`, `npm run build:api`, `node --check infra/lambda/booking-handler/index.mjs`, and JSON validation for edited Lex/Connect exports.
